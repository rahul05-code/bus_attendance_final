name: bus_attendance_app
description: A Flutter app for bus attendance using mobile_scanner and Google Sheets backend.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  mobile_scanner: ^3.5.0
  http: ^0.13.6
  shared_preferences: ^2.2.2

flutter:
  uses-material-design: true
